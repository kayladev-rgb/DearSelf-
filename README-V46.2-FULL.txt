DearSelf V46.2 Beta — Full Project

This package combines the latest V46.2 index.html/settings fixes with the latest notification service worker and push server from V46.1, plus the existing DearSelf project files (PWA manifest, icons, GitHub Pages workflow, sync bridge, package.json, and run.bat).

GitHub Pages files at the project root:
- index.html
- sw.js
- manifest.json
- version.json
- icon-192.png / icon-512.png and icons/

Push server:
- server.js
- package.json

For the push server, install dependencies with npm install and configure the VAPID environment variables before starting it.
